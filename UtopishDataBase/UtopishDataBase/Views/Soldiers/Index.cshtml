@model IEnumerable<UtopishDataBase.Soldier>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CauseType.SoldierTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CauseType.SoldierTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SoldierID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SoldierID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SoldierID })
        </td>
    </tr>
}

</table>
