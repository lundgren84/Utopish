@model IEnumerable<UtopishDataBase.BuildingType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BuildingTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BuildingTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BuildingTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BuildingTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BuildingTypeID })
        </td>
    </tr>
}

</table>
