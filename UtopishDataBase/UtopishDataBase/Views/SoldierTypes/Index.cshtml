@model IEnumerable<UtopishDataBase.SoldierType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SoldierTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SoldierTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SoldierTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SoldierTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SoldierTypeID })
        </td>
    </tr>
}

</table>
